local_dir: "root"
bin_dir: "/usr/bin"
config_dir: "/etc/kubernetes"
config_file: "scheduler"

cert_dir: "/etc/ssl/kubernetes"
ca_file: "ca.pem"
scheduler_certfile: "scheduler.pem"
scheduler_keyfile: "scheduler-key.pem"

log_dir: "/var/log/kubernetes"
log_file: "scheduler.log"
scheduler_config: "scheduler-config.yaml"
scheduler_kubeconfig: "kube-scheduler.kubeconfig"
